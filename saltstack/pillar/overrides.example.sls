# Required: replace with the single Vault root token given during the Vault
# setup process
vault token: "[[insert vault root token value here]]"

# Optional: custom cluster domain name - for example - rather than using the
# default 'devcluster', you can provide your own so you can access cluster 
# services on [service-name].service.[custom domain].
#cluster domain: "[[custom domain]]"